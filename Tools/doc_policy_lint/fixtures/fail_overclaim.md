# Fail fixture — affirmative non-goal overclaims (each line MUST be flagged)

- RuleDSL is a distributed workflow orchestration engine for your microservices.
- The engine provides a built-in network protocol for remote rule evaluation.
- RuleDSL implements distributed consensus across multiple nodes.
- The SDK serves as a persistence layer for your decision history.
- RuleDSL acts as an API gateway in front of your services.
